export const environment = {
  production: false,

  // Keep true so the app runs immediately without a backend.
  // Change this to false when your .NET Core API is ready.
  useMockData: true,

  // Change this URL to your real backend API endpoint.
  apiUrl: 'https://localhost:5001/api/policies'
};
